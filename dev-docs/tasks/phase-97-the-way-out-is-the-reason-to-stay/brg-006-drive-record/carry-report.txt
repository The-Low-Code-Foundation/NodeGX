Carry report — /var/folders/jb/nb63j64j3qvg0thnmcl7zmnh0000gn/T/brg006-drive-6bsfky/data/local.db
             -> postgres:///nodegx_brg006_73416_mu9v3w2a
  taken 2026-09-20T13:40:00.707Z, permissions read from security.json

  [carries ] collection: Follow
             {"rows":3}
  [carries ] index: Follow(userId)
  [carries ] row ACL: Follow
             {"user":3,"everyone":0,"role":0}
             Enforced by the app server in the SQL it builds, so it carries because
             the enforcement code carries — not because anything is written into
             PostgreSQL.
  [carries ] collection: Item
             {"rows":7}
  [carries ] unique index: Item(id)
  [carries ] index: Item(published)
  [carries ] index: Item(sourceId)
  [carries ] collection: Keep
             {"rows":1}
  [carries ] index: Keep(userId)
  [carries ] row ACL: Keep
             {"user":1,"everyone":0,"role":0}
             (as above)
  [carries ] collection: Mark
             {"rows":2}
  [carries ] index: Mark(itemId)
  [carries ] collection: Ping
             {"rows":2}
  [carries ] collection: Source
             {"rows":3}
  [carries ] internal table: _ApiKey
             {"rows":1}
             API keys (hashed). Invisible to `exportSchemas()` and `listTables()`,
             which both filter `_%`.
  [carries ] internal table: _Audit
             {"rows":16}
             the audit log. Invisible to `exportSchemas()` and `listTables()`, which
             both filter `_%`.
  [carries ] internal table: _EmailToken
             {"rows":0}
             password-reset and verification tokens. Invisible to `exportSchemas()`
             and `listTables()`, which both filter `_%`.
  [carries ] internal table: _Files
             {"rows":1}
             file RECORDS — the bytes live on disk beside the database, not in it.
             Invisible to `exportSchemas()` and `listTables()`, which both filter
             `_%`.
  [carries ] internal table: _HttpCache
             {"rows":1}
             an HTTP response cache — it rebuilds itself, so the data phase may skip
             it. Invisible to `exportSchemas()` and `listTables()`, which both filter
             `_%`.
  [carries ] relation junction table: _Join_users__Role
             {"rows":1}
             Carried as a table in its own right: the relation is its rows.
  [carries ] internal table: _Role
             {"rows":1}
             roles. Invisible to `exportSchemas()` and `listTables()`, which both
             filter `_%`.
  [carries ] internal table: _Schema
             {"rows":15}
             the schema the adapter itself tracks. Invisible to `exportSchemas()` and
             `listTables()`, which both filter `_%`.
  [carries ] internal table: _Session
             {"rows":2}
             live sessions — carrying it keeps everyone logged in across the move.
             Invisible to `exportSchemas()` and `listTables()`, which both filter
             `_%`.
  [carries ] internal table: _User
             {"rows":2}
             user accounts. Invisible to `exportSchemas()` and `listTables()`, which
             both filter `_%`.
  [carries ] internal table: _UserIdentity
             {"rows":0}
             OAuth identities. Invisible to `exportSchemas()` and `listTables()`,
             which both filter `_%`.
  [carries ] collection permissions: security.json (6 collections with their own rules)
             The same file governs the same app server after the move; nothing about
             it is database-specific.

  26 carry, 0 degraded, 0 refused — 17 tables, 58 rows.
  Nothing refuses to cross.
